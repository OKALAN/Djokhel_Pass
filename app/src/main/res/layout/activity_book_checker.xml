<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".bookChecker.bookChecker">

    <ListView
            android:id="@+id/HistoryListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

    </ListView>

   <RelativeLayout
            android:id="@+id/EmptyRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginTop="100dp"
            android:visibility="invisible">



       <ImageView android:layout_width="120dp"
                  android:layout_height="120dp"
                  android:src="@drawable/delete_ticket"
                  android:id="@+id/empty_history"/>

       <TextView android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:text="Pas de ticket(s)!"
                 android:textSize="@dimen/size_20"
                 android:layout_below="@+id/empty_history"
                 android:textColor="@color/colorPrimaryDark"/>




      <!-- <ImageView
                android:id="@+id/empty_history"
                android:layout_width="120dp"
                android:layout_height="120dp"
                app:srcCompat="@drawable/ic_empty_foldewrr"
                android:contentDescription="@string/icon"/>

        <TextView
                android:text="@string/empty_details"
                android:textColor="@color/colorPrimary"
                android:textSize="@dimen/text_normal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@+id/empty_history"/> -->
    </RelativeLayout>

</RelativeLayout>